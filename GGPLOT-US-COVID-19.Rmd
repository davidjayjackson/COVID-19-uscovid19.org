---
title: 'GGPLOT2: Us in a Nutshell'
author: "David Jackson"
date: "09/05/2020"
output: 
  html_document: 
    fig_width: 10
    fig_height: 7
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(RSQLite)
library(ggplot2)
library(scales)
library(plotly)
library(esquisse)
theme_set(theme_linedraw())
```

```{r}
rm(list=ls())
source("../DATA/movavg.R")
db <- db <- dbConnect(RSQLite::SQLite(),dbname= "../COVIDDB/COVID.sqlite3")
df <- dbGetQuery(db,"select * from JHUDATA")
df <- df[order(df$date,decreasing = TRUE),]
df <- subset(df,country =="US" & date >="2020-04-01" )
df$date <- as.Date(df$date)
str(df)
# dbDisconnect(db)
summary(df)
```
### GGPLOt2 Plots of Cumulative Cases and Deaths
#### Source: New York Times

```{r}
ggplot(df) +geom_line(aes(x=date,y=confirmed),lwd=3,col="red") + 
    scale_x_date(date_breaks = "1 month") +
  scale_y_continuous(labels = comma) +
   theme(axis.text.x=element_text(angle =- 45, vjust = 0.5)) +
  labs(title="US Cumulative Total Cases",
       x="Date Reported",y="Cumulative Cases") 
  ```
```{r}
ggplot(df) +geom_line(aes(x=date,y=deaths),lwd=3,col="blue") + 
    scale_x_date(date_breaks = "1 month") +
  scale_y_continuous(labels = comma) +
   theme(axis.text.x=element_text(angle =- 45, vjust = 0.5)) +
  labs(title="US Cumulative Total Deaths",x="Date Reported",y="Cumulative Deaths")
```
### GGPLOt2 Plots of Daily Deaths and Deaths
#### Source: European CDPC

```{r}
df1 <- dbGetQuery(db,"select * from ECDC")
df1 <- subset(df1,Countries =="USA" & Reported >="2020-04-01" )
df1$Reported <- as.Date(df1$Reported)
str(df1)
dbDisconnect(db)
```

```{r}
p1 <-ggplot(df1) +geom_line(aes(x=Reported,y=Cases,col="red"),lwd=2,col="red") + 
    scale_x_date(date_breaks = "1 month") +
  scale_y_continuous(labels = comma) +
   theme(axis.text.x=element_text(angle =- 45, vjust = 0.5)) +
  labs(title="US Daily Total Cases",x="Date Reported",y="Daily Cases")
```
```{r}
p2 <-ggplot(df1) +geom_line(aes(x=Reported,y=Deaths),lwd=2,col="blue") + 
    scale_x_date(date_breaks = "1 month") +
  scale_y_continuous(labels = comma) +
   theme(axis.text.x=element_text(angle =- 45, vjust = 0.5)) +
  labs(title="US Daily Total Deaths",x="Date Reported",y="Daily Deaths")

ggplotly(p1)
ggplotly(p2)
```
